classDiagram
    class Word {
        <<Abstract>>
        # string english
        # string vietnamese
        # vector~string~ synonyms
        # vector~string~ antonyms
        # vector~string~ examples
        # int leitnerBox
        + Word(string en, string vi)
        + ~Word()
        + getEnglish() string
        + getVietnamese() string
        + getLeitnerBox() int
        + setLeitnerBox(int box) void
        + displayDetails()* void
    }

    class GeneralWord {
        - string partOfSpeech
        + GeneralWord(string en, string vi, string pos)
        + displayDetails() void
    }

    class TechnicalTerm {
        - string field
        + TechnicalTerm(string en, string vi, string field)
        + displayDetails() void
    }

    class Idiom {
        - string origin
        + Idiom(string en, string vi, string origin)
        + displayDetails() void
    }

    class Dictionary {
        - map~string, Word*~ dictionaryMap
        + Dictionary()
        + ~Dictionary()
        + addWord(Word* word) bool
        + searchWord(string key) Word*
        + removeWord(string key) bool
        + getWordsByLeitnerBox(int box) vector~Word*~
    }

    Word <|-- GeneralWord : Inherits
    Word <|-- TechnicalTerm : Inherits
    Word <|-- Idiom : Inherits
    Dictionary "1" o-- "*" Word : Manages (Aggregation)